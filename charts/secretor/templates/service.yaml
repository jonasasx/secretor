apiVersion: v1
kind: Service
metadata:
  name: {{ include "secretor.fullname" . }}
  labels:
    {{- include "secretor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secretor.selectorLabels" . | nindent 4 }}
